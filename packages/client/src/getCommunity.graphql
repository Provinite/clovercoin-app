query getCommunity($filters: CommunityFilters!) {
  community(filters: $filters) {
    __typename
    ... on Community {
      id
      name
    }
    ... on NotFoundError {
      message
    }
    ... on BaseError {
      message
    }
  }
}
