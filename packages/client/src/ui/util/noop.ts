/**
 * A function to be used to do nothing. Primarily here
 * for documentation purposes. Just makes it a bit more
 * obvious that something is intentionally a noop.
 *
 * @returns undefined
 */
export const noop = () => undefined;
